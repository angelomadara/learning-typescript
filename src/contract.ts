interface User {
    id: number;
    firstName: string;
    lastName: string;
    isAdmin: boolean;
}

const onlineUser: User = {
    id: 1,
    firstName: "angelo",
    lastName: "madara",
    isAdmin: true,
};
